# Resultados reproducibles

- `benchmark-latest.txt` se genera al ejecutar el benchmark.
- `audit.md` contiene la auditoría manual y el conteo de código junior.
- Los tests son un simulador mínimo de APIs de WoW para detectar regresiones de lógica; no reemplazan una prueba dentro del cliente Retail/PTR.
